include <fstream>
#include <iostream>
using namespace std;

int main () {

   for (int i=0; i < 3; i++){
   
   char arr[100];
   ofstream outfile;
   outfile.open("ameera.txt");
   cout << "Enter Candidate name: ";
   cin.getline(arr, 100);
   outfile << arr << endl;
   cout << "Enter Candidate age: ";
   cin >> arr;
   cin.ignore();
   outfile << arr << endl;
   outfile.close();
   ifstream infile;
   infile.open("ameera.txt");
   infile >> arr;
   cout << arr << endl;
   infile >> arr;
   cout << arr << endl;
   infile.close();
    }
    
   return 0;
}
